use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "ha-local-production-search-key"
  limiter: false
  image_proxy: false

search:
  safe_search: 1
  formats:
    - html
    - json
